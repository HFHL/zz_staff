<!--index.wxml-->
<scroll-view class="scrollarea" scroll-y type="list">
  <view class="container">
    <view class="header">
      <text class="welcome">欢迎使用</text>
    </view>

    <view class="button-group">
      <button class="btn" bindtap="onBluetoothTest">蓝牙测试</button>
    </view>
  </view>
</scroll-view>
const services = await bleManager.getBLEDeviceServices(deviceId);
// 遍历服务找到可写的特征值const services = await bleManager.getBLEDeviceServices(deviceId);
// 遍历服务找到可写的特征值const services = await bleManager.getBLEDeviceServices(deviceId);
// 遍历服务找到可写的特征值const services = await bleManager.getBLEDeviceServices(deviceId);
// 遍历服务找到可写的特征值